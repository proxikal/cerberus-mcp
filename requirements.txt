typer>=0.9.0
pydantic>=2.0.0
pytest>=7.0.0
loguru>=0.7.0
pathspec>=0.12.0
setuptools
sentence-transformers>=2.6.0
numpy>=1.26.0

# Phase 2: Context Synthesis & Compaction
tree-sitter>=0.20.0
tree-sitter-python>=0.20.0
tree-sitter-javascript>=0.20.0
tree-sitter-typescript>=0.20.0
tree-sitter-go>=0.20.0

# Phase 2: Auto-Summarization (Optional)
# ollama-python>=0.1.0  # Uncomment if using local LLM

# Phase 3: Operational Excellence
watchdog>=3.0.0  # Filesystem monitoring for background watcher
rank-bm25>=0.2.2  # BM25 keyword search for hybrid retrieval
psutil>=5.9.0  # Process management for daemon

# Phase 11: The Surgical Writer
black>=23.0.0  # Required for auto-formatting injected code blocks

# Phase 4: Aegis-Scale Performance (SQLite + FAISS)
# Note: FAISS is OPTIONAL for Cerberus Light (small-medium projects)
# For Cerberus Enterprise (10K+ files), install with:
#   pip install faiss-cpu>=1.7.4  # CPU version
#   pip install faiss-gpu>=1.7.4  # GPU version (production)
